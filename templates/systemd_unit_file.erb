[Unit]
Description=Google OAuth2 proxy for <%= @name %>
After=syslog.target

[Service]
Type=simple
ExecStart=/usr/bin/google_auth_proxy -config /etc/google_auth_proxy/<%= @name %>.cfg
KillSignal=SIGINT
Restart=on-failure
StandardError=syslog
NotifyAccess=all

[Install]
WantedBy=multi-user.target